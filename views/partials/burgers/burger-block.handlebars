<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newBurger="{{devour}}">
		{{#if devour}}FOOD TIME!{{else}}choose a burger!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
